using Microsoft.UI.Xaml.Controls;

namespace KozmoTech.ZenUtility.ChecksumHasher.UI.Controls;

public sealed class AppBarDropDownButton : AppBarButton
{
    public AppBarDropDownButton()
    {
        DefaultStyleKey = typeof(AppBarDropDownButton);
    }
}
