using Microsoft.UI.Xaml.Controls;

namespace KozmoTech.ZenUtility.ChecksumHasher.UI;

/// <summary>
/// A page used to verify hash codes.
/// </summary>
public sealed partial class HashVerifierPage : Page
{
    public HashVerifierPage()
    {
        InitializeComponent();
    }
}
